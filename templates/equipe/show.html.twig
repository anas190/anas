{% extends 'front.html.twig' %}

{% block title %}Equipe{% endblock %}

{% block body %}
    <h1>Equipe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdEquipe</th>
                <td>{{ equipe.idEquipe }}</td>
            </tr>
            <tr>
                <th>NomEquipe</th>
                <td>{{ equipe.nomEquipe }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_equipe_index') }}">back to list</a>

    <a class="btn btn-success" href="{{ path('app_equipe_edit', {'idEquipe': equipe.idEquipe}) }}">edit</a>

    {{ include('equipe/_delete_form.html.twig') }}
{% endblock %}
