<form method="post" action="{{ path('app_invitation_delete', {'idInvitation': invitation.idInvitation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ invitation.idInvitation) }}">
    <button class="btn btn-danger">Delete</button>
</form>
