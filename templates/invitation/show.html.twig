{% extends 'front.html.twig' %}

{% block title %}Invitation{% endblock %}

{% block body %}
    <h1>Invitation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdInvitation</th>
                <td>{{ invitation.idInvitation }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ invitation.etat }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_invitation_index') }}">back to list</a>

    <a class="btn btn-success" href="{{ path('app_invitation_edit', {'idInvitation': invitation.idInvitation}) }}">edit</a>

    {{ include('invitation/_delete_form.html.twig') }}
{% endblock %}
